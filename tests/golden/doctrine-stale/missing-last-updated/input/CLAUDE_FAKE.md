# CLAUDE_FAKE.md

**Owner:** Engineering Council

> This is a synthetic doctrine file that intentionally OMITS the
> `**Last Updated:**` metadata line. Per
> `docs/operations/RDG_GOVERNANCE_REPORTS_PLAYBOOK.md` and
> `CLAUDE.md` §8 ("Documentation Metadata"), every doctrine doc
> MUST carry `Last Updated:` and `Status:` headers. RDG's
> `content-doctrine.rdg` pipeline is expected to flag this.

## Some Doctrine

This section purports to describe a doctrinal rule, but the
absence of the freshness metadata means readers cannot tell
whether the rule is current or superseded. That is the failure
mode this fixture demonstrates.

## More Doctrine

Lossless retention applies to every doctrine doc: reductions
ARCHIVE with provenance per the REDUCTION_LEDGER. This is
correctly described here, but without `Last Updated:` the doc
itself is ironically stale-unflagged.
