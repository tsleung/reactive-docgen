# Golden Verdict — math-violations/null-coalescing-in-financial-code
**Generated by:** rdg-notebook/reactive-docgen/tests/run-golden.py
**Source-of-truth inputs:** tests/golden/math-violations/null-coalescing-in-financial-code/input/policy-lookup.ts
**Last regenerated:** fixture-static
**Schema version:** 1.0
**Do not hand-edit** — re-run the script.

<!-- machine-readable
{
  "schemaVersion": "1.0",
  "generatedAt": "fixture-static",
  "data": {
    "fixtureId": "math-violations/null-coalescing-in-financial-code",
    "rdgPipeline": "code-health.rdg",
    "promptFile": ".rdg/prompts/code-health.md",
    "expectedFindings": [
      {
        "severity": "P0",
        "category": "Safety Net Patterns",
        "file": "policy-lookup.ts",
        "line": 25,
        "patternMatch": "?? 0.6",
        "description": "Silent fallback after policy lookup hides missing tensor entries; per §10 Boundary Rule, internal lookups must fail loudly."
      }
    ],
    "expectedVerdict": "FAIL",
    "negativeAssertions": [
      "No P0 finding should describe this as a legitimate default parameter",
      "No P0 finding should accept this as a documented mathematical exception"
    ]
  }
}
-->

## Human-readable summary

This fixture plants a single `??` operator on `policy[t]?.[wealthIndex]?.[humanCapitalIndex] ?? 0.6` inside a financial-engine file. The `code-health.rdg` pipeline MUST emit a `P0 Safety Net Patterns` finding pointing at line 25 of `policy-lookup.ts`.

### What this fixture demonstrates
- The most common silent-fallback shape in the RtB codebase: `optional?.chain?.access ?? defaultValue`.
- A bug that produces missing tensor entries would substitute `0.6` invisibly; RDG must flag this.

### What would falsify the fixture
- An RDG run that returns "PASS / no findings" or only flags this as `P1`/`P2`.
- An RDG run that accepts the fallback as a legitimate "default allocation parameter" — that interpretation contradicts the prompt's safety-net language.

### Gating pipeline
`code-health.rdg` (prompts/code-health.md §2 "Safety Net Patterns (HIGH PRIORITY)").
